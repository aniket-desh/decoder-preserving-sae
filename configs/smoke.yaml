experiment: smoke
seed: 0

representation:
  center: true
  ridge: 1.0
  decoder_reduction: mean
  task_prior: isotropic

sae:
  architecture: batch_topk
  expansion_factor: 8
  average_l0: 32

objective:
  mse_weight: 1.0
  decoder_weight: 0.1
  auxiliary_weight: 1.0

report:
  - activation_mse
  - explained_variance
  - average_l0
  - dead_feature_rate
  - heldout_decoder_distance
  - heldout_probe_score
  - output_kl

